# PMP-4 — Validation and test contracts

Status: completed

Implemented:
- validation contract for approved downstream-playbook mini-prompts;
- mandatory authority, integrity, context, fallback and approval checks;
- mandatory test scenario model;
- validation report and test catalog templates;
- blocking rules before prompt activation in a release candidate.

Not implemented yet:
- canonical APF flow/template integration (PMP-5);
- real playbook pilot;
- live-model or multi-runtime replay;
- internal APF mini-prompts.
