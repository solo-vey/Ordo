# CSG-A1 — Language baseline schema and package-binding alignment

Status: completed

APF rc.17 CSG behavior was preserved and aligned to the canonical Ordo CSG baseline.

Aligned areas:
- `DEVIATION.CLASSIFY` record schema;
- confidence enum;
- escalation counter scopes;
- package binding and decision statuses;
- canonical generated-playbook artifact names;
- dedicated trace-events artifact;
- CSG metadata statuses.

No APF internal CSG activation was introduced. No business behavior changed.
