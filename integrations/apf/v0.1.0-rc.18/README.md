# APF v0.1.0-rc.18 — CSG Language Baseline Aligned Confirmed Baseline

This package is the confirmed APF baseline aligned with `ORDO_LANGUAGE_BASELINE_WITH_CSG_v0_1`.

## Included alignment

- canonical `DEVIATION.CLASSIFY` records;
- canonical confidence values;
- canonical escalation counter scopes;
- canonical CSG package binding and `decision_status`;
- canonical generated-playbook CSG artifact names;
- dedicated CSG trace-events artifact;
- regression validation for aligned behavior.

## Runtime boundary

APF designs, packages, and validates optional CSG capability for generated playbooks. APF does not enable CSG for itself by default and does not replace runtime behavior.

## Status

- previous confirmed baseline: `v0.1.0-rc.17-conversation-scope-guard-support-confirmed-closure`;
- confirmed baseline: `v0.1.0-rc.18-csg-language-baseline-aligned-confirmed-closure`;
- blockers: `0`;
- human confirmation: accepted on `2026-07-11`.
