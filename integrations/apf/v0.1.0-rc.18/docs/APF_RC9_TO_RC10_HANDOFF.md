# APF rc.9 to rc.10 Handoff

## Current accepted working baseline

`APF_TRANSFER_PACKAGE_CURRENT_STATE_RC9_REAL_MODULE_TESTCASE_GENERATION.zip`

## Recommended next APF patch

`APF rc.10 — Testcase generation smoke/spec validation closure`

Potential scope:

- validate testcase target manifest against schema;
- validate scenario catalog against schema;
- validate expected behavior matrix against schema;
- check scenario coverage against required scenario groups;
- ensure generated testcase plan does not claim runtime execution;
- preserve APF/language scope boundary.
