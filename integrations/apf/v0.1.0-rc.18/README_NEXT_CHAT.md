# README NEXT CHAT

This package is APF `v0.1.0-rc.18-csg-language-baseline-aligned-confirmed-closure`.

Use it as the active confirmed baseline for subsequent APF work. Conversation Scope Guard is language-aligned and remains disabled for APF itself by default.
