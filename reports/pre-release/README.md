# Pre-release reports

This directory contains repository-level pre-release evidence retained for provenance.

- `legacy-root/` preserves records that were formerly stored in the repository root.
- These files describe historical pre-release states and must not be interpreted as the current release status.
- Current release evidence is produced by the delivery and self-check workflows in their dedicated report directories.
