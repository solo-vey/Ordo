# M88.1 — Canonical ANTIPATTERN.DEF schema and initial registry

Status: **passed**

Implemented:

- canonical `ANTIPATTERN.DEF` JSON Schema;
- severity: `info | warning | error | critical`;
- enforcement: `advisory | blocking`;
- structured detection metadata;
- recovery, remediation and evidence fields;
- initial registry with **6 anti-patterns**;
- standalone registry validator;
- regression tests.

Validation: **11/11 tests passed**.
