# M88.2 — Canonical DETECT.RULE schema and detector contract

Status: **passed**

Implemented:

- canonical `DETECT.RULE` JSON Schema;
- typed detection input contract;
- deterministic condition operators: `all`, `any`, `threshold`;
- canonical predicate set;
- `DetectionContext`, `DetectionEvidence` and `ANTIPATTERN.FINDING`;
- canonical detector protocol and implementation;
- initial registry with **6 detection rules**;
- complete rule coverage for all six initial anti-patterns;
- registry and contract regression tests.

Validation: **24/24 tests passed**.
