# M88.4 — Runtime detectors for the initial anti-pattern set

Status: **passed**

Implemented:

- runtime signal extractors for all six initial anti-patterns;
- canonical runtime orchestrator;
- single and batch evaluation;
- context-aware rule filtering;
- materialized `ANTIPATTERN.FINDING` records;
- aggregated runtime `GATE.REPORT`;
- CLI entrypoint;
- positive and negative runtime fixtures.

Validation: **48/48 tests passed**.
