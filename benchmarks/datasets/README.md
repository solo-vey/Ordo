# Benchmark datasets

This directory contains versioned JSON task sets for CSG adversarial evaluation
and shared Ordo-versus-plain-prompt comparisons.

The matching validation schemas are in [`../schemas/README.md`](../schemas/README.md);
the execution harnesses are in [`../ab/README.md`](../ab/README.md).
