# START PROMPT — Vibe ARF Bootstrap

This file is the **only root bootstrap entry** for the Vibe ARF package.

1. Read `START_PROMPT_RUNTIME_MODE.md`.
2. Execute it exactly: load `START_HERE_RUNTIME_MODE.md`, use the packaged Ordo CLI, and derive the next step from runtime evidence.
3. Do **not** start authoring from README, memory, raw `compiled/*`, or by manually walking the graph.
4. Do **not** ask the analyst the static intake text from package documentation. Ask only the question returned by the runtime when a human answer is actually required.
5. If the packaged runtime cannot execute, stop and report the runtime blocker; do not improvise the workflow.

Bootstrap rule: **runtime first, process second, analyst question only when runtime authorizes it.**
