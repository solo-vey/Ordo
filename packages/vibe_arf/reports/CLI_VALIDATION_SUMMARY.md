# CLI validation summary

CLI status: not_run

This is the repository-native Vibe ARF release template. It records no CLI
execution claim. A runtime or release workflow must replace it with evidence
for the commands it actually executed.
