# START HERE — Vibe ARF 0.1.2

Use canonical Ordo as semantic authority and the persisted Information-First/Authority-First authoring model as the Vibe design method.

Do **not** start by inventing the process tree. Start with desired artifacts, decompose them into information objects, build and validate the typed information-flow model, derive analyst-minimal review bundles and authority boundaries, quarantine proposals from canonical truth, and only then synthesize the Ordo execution graph as a projection.

Before an analyst-ready generated candidate may be exposed, the machine-detectable contour must be green:

- AIM validity;
- AIM ↔ Ordo projection binding;
- review-bundle design;
- proposal/canonical separation;
- approval-ledger persistence and immediate local persistence gates;
- state/postcondition ordering;
- causal recovery locality;
- semantic input/dependency parity;
- explicit behavioral scenario coverage;
- pinned-runtime simulation evidence and fixture closure;
- non-vacuous runtime gate evidence;
- defect-ownership classification;
- all ordinary PRE_EDITOR language/profile/package checks.

For runnable candidates, execute the exact candidate ZIP with the pinned Simulation Kit **before analyst visibility**. A runtime-adapter defect is handed to that adapter with evidence; do not mutate a canonical-valid playbook as a workaround.

Human questions are reserved for consequential user-owned decisions. Proposal generation, grouping, fixture synthesis, machine repair and adapter triage are autonomous while their contracts are mechanically checkable.

Vibe itself is self-hosted: its `authoring/` information model and `ordo_projection.yaml` are verified by the same AIM/projection rules it applies to generated playbooks.

Pinned simulation adapter: Simulation Kit `0.1.5` / runtime `0.2.0-alpha.20.0.166-dev`. Treat `fixture_incomplete` as an exact fixture-discovery loop; preserve generated missing-fixture contracts and rerun before analyst visibility.
