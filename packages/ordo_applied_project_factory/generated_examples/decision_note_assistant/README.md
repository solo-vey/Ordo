# M61.2 Generated Example — ordo.decision_note_assistant

Purpose: validate that `ordo.applied_project_factory` can produce a first applied-project `source/program.ordo.yaml` from a small imaginary process.

Scope:

```text
included: source/program.ordo.yaml, SVG review views, validation reports
excluded: full runtime/dev/evidence package, generated markdown outputs, confusion tests
```

Validation evidence was produced in a temporary Ordo package scaffold so the generated output itself remains source-only.

Checks:

```text
lint: passed
compile: passed
test: passed
coverage: passed
```

SVG views:

```text
visuals/decision_note_main_tree.svg
visuals/decision_note_tree_with_gates.svg
visuals/decision_note_tree_with_artifacts.svg
visuals/decision_note_needs_clarification_context.svg
```
