# Release manifests

This directory groups release-related manifests by lifecycle role.

- `legacy-root/` contains historical release and change-bundle manifests formerly stored in the repository root.
- Active release identity and version-state contracts remain in the canonical `manifests/` paths used by release tooling.
