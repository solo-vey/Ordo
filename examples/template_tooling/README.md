# Template tooling example

This directory contains a small deterministic template registry, source templates,
render input, and validation reports used as an example of template tooling.

For user onboarding, return to [`../README.md`](../README.md). For package
templates, see [`../../cli/README.md`](../../cli/README.md).
