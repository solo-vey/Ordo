# Internal regression fixtures only

These files are not valid runtime outputs for an external scenario execution. Do not copy them into `outputs/`. Generate documents from the current canonical runtime state.
