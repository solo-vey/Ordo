# Provenance and Immutability

Files under `raw_evidence/` are immutable after registration. Every run references the raw path and SHA-256. Derived measurements, findings and claims are separate artifacts with explicit source references. No downstream artifact may rewrite upstream evidence.
