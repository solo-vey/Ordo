# Behavioral mode selection

After the deterministic regression, ask the user one question:

> Which package should be prepared for behavioral testing?
>
> 1. `provider_api` — live API runs with provider provenance.
> 2. `chat_native` — execution in one or more separate chats.
> 3. `skip_behavioral` — finish with the deterministic report only.

After the answer, do not request another confirmation.
