# Ordo Tree Editor 0.2.0-alpha.20.0.178-dev

- Node inspector treats `title` as a known field.
- `title` is rendered first in the known-fields list.
- Node graph label priority remains `title -> question -> purpose -> ...`.
