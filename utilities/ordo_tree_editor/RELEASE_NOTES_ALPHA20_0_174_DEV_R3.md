# Ordo Tree Editor 0.2.0-alpha.20.0.174-dev

- Retires the legacy Reconstructed Flow view from Show Data Flow.
- Source Data Flow is now the only/default Data Flow workspace.
- Keeps Dependency Tree, Variable Passports, source inspector and model explanation.
- Packages without an embedded authoring data-flow show an explicit empty/error state; no reconstructed fallback is used.
- Updates Help and UI regression coverage for the source-only contract.
