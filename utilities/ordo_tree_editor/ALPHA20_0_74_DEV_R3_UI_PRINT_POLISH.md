# alpha.20.0.74-dev — UI and print polish

UI-only changes:
- Resource Preview Close stays right-aligned.
- Initial Model Settings button height matches Upload buttons.
- Markdown reference resources have Preview / Source tabs.
- Model explanation actions become disabled after a successful explanation to prevent accidental repeated model calls.
- Replay Real Chat print output uses continuous-flow pagination; replay steps and long responses may split across pages instead of forcing whole cards to the next page.

No execution/runtime/compiler/playbook semantic changes.
