# alpha.20.0.64-dev — inline package resource preview

- Resolved References cards are interactive.
- Clicking a text package resource opens a read-only inline preview below the reference list.
- Preview preserves inspector/node context and can be closed without changing selection.
- Keyboard activation is supported.
- Missing references remain non-interactive.

UI-only change; no runtime/playbook semantics.
