# alpha.20.0.78-dev — Replay print analyst parity

UI/print-only fix.

- Print/PDF preserves analyst-response background with print-color-adjust.
- Analyst responses also get a thin neutral border so they remain visibly distinct even when a print driver suppresses backgrounds.
- Structured accepted-decision values inherit the same analyst container treatment.
- Replay DOM and runtime semantics are unchanged.
