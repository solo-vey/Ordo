# alpha.20.0.56-dev — generic template/resource inspector

- Template-capable execution nodes expose a read-only right-side inspector.
- Tabs: Overview, Template, Parameters, References, Raw.
- Template and bindings text are resolved from the currently loaded playbook package.
- Missing resources are shown explicitly.
- The implementation is resource/contract based and contains no domain-specific semantics.
