# Ordo Tree Editor 0.2.0-alpha.20.0.176-dev

- Node display-label priority is now `title -> question -> purpose` before existing fallbacks.
- Gate display-label priority is unchanged.
- Dialog/path node labels use the same priority as the main graph.
