# alpha.20.0.120-dev

- Single click selects a node only for Inspector details.
- Double click sets the focused lineage root and rebuilds the graph.
- Inspector selection and focused lineage root are separate.
- Free layout remains draggable inside a focused subgraph.
- Edge hover highlights the exact edge and its two endpoint nodes while dimming unrelated graph elements.
- Clicking empty graph space clears the focused projection and restores the full graph.
