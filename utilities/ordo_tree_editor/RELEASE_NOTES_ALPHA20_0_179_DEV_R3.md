# Ordo Tree Editor 0.2.0-alpha.20.0.179-dev

- Adopted the functional changes present in the supplied `.178-dev` candidate as the new integrated baseline.
- Preserved Playbook Settings unbound-resource subtabs introduced after `.174-dev`.
- Preserved node display-label priority `title -> question -> purpose -> ...` across graph, dialog/path projection, and backend projection.
- Preserved Node Inspector handling of `title` as a known field rendered first.
- Fixed package release metadata consistency: `VERSION` now matches `.179-dev`.
- Corrected the historical `.176-dev` release-note header that incorrectly identified itself as `.177-dev`.
- Restored the missing root-level `.177-dev` release note from the shipped editor release history.
- Canonical Ordo, compiler semantics, and runtime execution semantics are unchanged.
