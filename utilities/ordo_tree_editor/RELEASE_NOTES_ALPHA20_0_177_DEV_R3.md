# 0.2.0-alpha.20.0.177-dev

- Fixed node display-label priority consistently across graph/live display and backend projection.
- Node labels now use `title -> question -> purpose` before existing fallbacks.
- Added regression coverage for title-over-question precedence and question fallback.
