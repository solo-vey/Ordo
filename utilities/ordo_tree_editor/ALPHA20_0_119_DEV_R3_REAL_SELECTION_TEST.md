# alpha.20.0.119-dev

Focused Data Flow is validated against the actual RISK_FACTOR_PASSPORT 0.9.5 lineage fixture. Same-layer siblings are never rendered, and downstream consumer co-input expansion has been removed.
