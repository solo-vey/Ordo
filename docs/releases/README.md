# Release notes

This directory groups repository-level release notes retained for provenance.

- `legacy-root/` contains historical release notes formerly stored in the repository root.
- [`../release_notes.md`](../release_notes.md) documents the generated package release-note workflow.
- Current release identity remains defined by `manifests/RELEASE_IDENTITY.json` and `manifests/VERSION_STATE.json`.
