# Handoff Note

This package is ready to be kept next to the Ordo language package as an improvement-intake artifact.

## Use it when

```text
- starting the next Ordo language milestone;
- deciding what APF discoveries should become language standards;
- planning M64 or equivalent post-APF language line;
- avoiding direct opcode/IR promotion without evidence.
```

## Do not use it as

```text
- a replacement for APF runtime package;
- a compiled Ordo release;
- a proof that language runtime already supports all listed concepts.
```

## Immediate next work outside this package

Continue APF with:

```text
FUTURE-PROGRAM-CONTRACT-01 — program-level metadata, interaction semantics and approval gates
```

After that APF patch is implemented, update this language pack with evidence from actual source YAML, compiled IR, validation reports and generated package README/start prompt.
