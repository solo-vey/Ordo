# M70.2 Language / CLI Root Contract Integration Report

Status: implemented / passed-validation.

Implemented a read-only `root_contract` treatment in `repo-check --clean`; adopted it for release-blocking `language/` and `cli/` roots; preserved package-level clean-check and delegated applied packages.
