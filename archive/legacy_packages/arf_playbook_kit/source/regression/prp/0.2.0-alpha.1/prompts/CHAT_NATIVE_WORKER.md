# CHAT-NATIVE WORKER PROMPT

This chat is an evidence-producing worker for a PRP campaign.

Read the supplied mode-specific test package. Execute only the assigned scenarios and repetitions. Keep baseline and candidate isolated. Record the user-selected model as declared metadata, not provider-verified provenance. Create normalized traces and return a results ZIP with SHA-256. Do not issue the final promotion verdict.
