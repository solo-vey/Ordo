# Mode 5 Migration Working Artifacts

This directory contains reusable working-document templates for the guided
legacy-instruction migration route (Project Factory mode 5).

For every artifact, follow: `collect -> save -> review -> gate -> checkpoint -> next`.
Save completed copies under `workspace/migration/` in the extracted ARF kit.
Keep original sources and unresolved items visible; never silently replace
missing information with assumptions.
