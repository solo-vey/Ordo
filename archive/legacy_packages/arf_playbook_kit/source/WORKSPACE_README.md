# ARF Workspace

Create the new playbook and its user-owned supporting material in this directory.

Keep runtime files, contracts, source modules, and compiled artifacts unchanged. The ARF package may generate validation and output material here, but it must not overwrite user work without an explicit instruction.

When continuing in another chat, provide this workspace together with the same ARF package or export the completed playbook as instructed by the runtime.
