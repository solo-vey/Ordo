# Archived handoffs

This directory preserves localized and historical handoff snapshots that are not current documentation entry points.

- `legacy-root/` contains handoff files formerly stored in the repository root.

Files in this contour are provenance. Current documentation starts at `README.md` and `docs/README.md`.
